services:
  - type: web
    name: bot-simulador
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: TICK_SECONDS
        value: "2"
      - key: MAX_BOTS
        value: "100"
